{
  "name": "muffin-time",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "sync-cards": "node scripts/sync-cards.js"
  },
  "devDependencies": {
    "vitest": "^2.1.1"
  }
}
