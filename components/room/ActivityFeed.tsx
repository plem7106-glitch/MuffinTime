'use client';

import { usePresentation } from '../../lib/presentation/presentationContext';

export function ActivityFeed() {
  // Underlying activityFeed tracking is preserved in PresentationContext.
  // Top-right status toast UI is removed for clean layout.
  return null;
}
